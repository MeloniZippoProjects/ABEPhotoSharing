<UserControl x:Class="KPClient.TagsSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0">
            Specify tags
        </Label>
        <TextBox
            Grid.Row="1"
            Name="TagsTextBox"
            Margin="5"
            MinHeight="30"
            TextChanged="TagsTextBox_OnTextChanged" />

        <TextBlock
            Grid.Row="2"
            Name="UniverseReminder"
            Margin="5" />
    </Grid>
</UserControl>